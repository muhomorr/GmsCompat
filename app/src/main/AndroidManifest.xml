<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="org.grapheneos.gmscompat"
>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />

    <application
        android:name=".App"
        android:label="@string/app_name"
        tools:ignore="AllowBackup,MissingApplicationIcon"
    >
        <service
            android:name=".PersistentFgService"
            android:exported="true"
            android:directBootAware="true"
            android:process="@string/persistent_process"
            android:permission="com.google.android.providers.gsf.permission.WRITE_GSERVICES"
        />
        <!-- WRITE_GSERVICES is a signature-protected permission held by Play Services and Play Store -->

        <provider
            android:name=".RpcProvider"
            android:authorities="${applicationId}.RpcProvider"
            android:exported="true"
            android:directBootAware="true"
            android:process="@string/persistent_process"
            android:permission="com.google.android.providers.gsf.permission.WRITE_GSERVICES"
        />

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@android:style/Theme.DeviceDefault.DayNight"
        >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name=".GserviceBroker"
            android:exported="true"
            android:directBootAware="true"
            android:process="@string/gservices_process"
            tools:ignore="ExportedService"
        />

        <service
            android:name=".location.FgService"
            android:exported="false"
            android:directBootAware="true"
            android:process=":gservices"
            android:foregroundServiceType="location"
        />

        <receiver
            android:name=".ResetGservices"
            android:exported="false"
            android:process="@string/gservices_process"
        />
    </application>
</manifest>
